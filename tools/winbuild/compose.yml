services:
  build:
    image: win-hedgewarsbe
    working_dir: /
    volumes:
      - ../../:/src
      - ./out:/build-Hedgewars
    command: ["wine", "cmd", "/c", "/src/tools/winbuild/build-hedgewars.cmd"]


